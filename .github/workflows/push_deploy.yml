name: 🔗 手动部署 GitPage

on: 
    workflow_dispatch:
  
# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  # 构建工作
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Show the timezone
        run: |
           date 
           uname -r
           free -hg
           pwd
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # lastUpdated 需要
          submodules: recursive
      - name: Build inside FreeBSD VM
        uses: vmactions/freebsd-vm@v1.3.8
        with:
          release: "15.0"
          arch: x86_64
          sync-time: true
          mem: 10240
          run: |
            pkg update -f 
            ls -l /home/runner/work/FreeBSD-Ask.github.io/FreeBSD-Ask.github.io/
            pwd
            echo "222" > 1.txt
            cp 1.txt /home/runner/work/FreeBSD-Ask.github.io/FreeBSD-Ask.github.io/docs/
            
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
          
      - name: 复制首页
        run:  |
          ls /home/runner/work/FreeBSD-Ask.github.io/FreeBSD-Ask.github.io
          pwd

        
